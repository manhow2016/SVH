/**
 * SVH Web 根组件（Phase 1 占位；Phase 8 替换为 WorkbenchLayout）。
 */
export default function App() {
  return (
    <div style={{ padding: 24, fontFamily: "monospace", color: "var(--color-text-primary)" }}>
      <h1>SVH — Short Video Harness</h1>
      <p>Agent Harness / Workspace / Session Management</p>
    </div>
  );
}
